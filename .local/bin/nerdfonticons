#!/bin/sh

icons_file="$HOME/.local/share/symbols/nerdfont_icons"

chosen=$(cat "$icons_file" | dmenuwrap -i -l 30 -p Nerdfont\ icons: | cut -d ' ' -f 1 | tr -d '\n')

[ -n "$chosen" ] && printf "$chosen" | xclip -selection clipboard && notify-send "'$chosen ' copied to clipboard."
